<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Vacinas cadastradas
            </div>
            <div class="panel-body">
                @if (ViewBag.Vacinas != null && ViewBag.Vacinas.Count > 0)
                {
                    <div class="table-responsive dataTable_wrapper">
                        <table class="table table-striped table-bordered table-hover" id="dataTables-vacinas">
                            <thead>
                                <tr>
                                    <th>Nome</th>
                                    <th>Anotações</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @for (int i = 0; i < ViewBag.Vacinas.Count; i++)
                                {

                                    <tr class="@(i % 2 == 0 ? "odd" : "even") gradeA">
                                        <td>@ViewBag.Vacinas[i].Nome</td>
                                        <td>@SysPec.App.Helpers.Helper.GetSubString(ViewBag.Vacinas[i].Anotacoes, 80)</td>
                                        <td class="text-right">
                                            <a href="@Url.Action("Index", new { VacinaId = ViewBag.Vacinas[i].Id })" class="btn btn-primary btn-circle">
                                                <i class="fa fa-edit"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <script>
                        jQuery(function () {
                            $('#dataTables-vacinas').DataTable();
                        });
                    </script>
                }
                else
                {
                    <div class="row">
                        <div class="col-lg-12">
                            <div class="alert alert-warning" role="alert">
                                <span class="glyphicon glyphicon-warning-sign" aria-hidden="true"></span>
                                <span class="sr-only">Atenção:</span>
                                Não existe vacinas cadastrados.
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
